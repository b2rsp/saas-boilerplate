import { atom, selector, useRecoilState, useRecoilValue } from "recoil";

function AboutPageContainer(state) {
  return {
    atom: {},
    helpers: {},
    actions: {} // We cant use the actions because of the hooks
  };
}
export { AboutPageContainer };
